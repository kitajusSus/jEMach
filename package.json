{
  "name": "jemach",
  "version": "1.0.0",
  "description": "TypeScript bindings for jemach Julia Native library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build ./src/cli.ts --outdir ./dist --executable --target bun",
    "watch": "tsc --watch",
    "example": "npm run build && node dist/example.js",
    "test": "echo \"No tests yet\" && exit 0",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "julia",
    "neovim",
    "jemach",
    "grammar"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.24",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.1",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "commander": "^11.1.0"
  }
}
